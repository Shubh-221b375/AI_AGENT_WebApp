Flask==2.2.3
openai==0.27.0
gunicorn==20.1.0
flask-cors==3.1.1
requests==2.28.1
python-dotenv==0.20.0


Flask: Framework for your backend.

openai: To interact with the OpenAI API.

gunicorn: A production server to run your Flask app.

flask-cors: To handle cross-origin resource sharing (CORS) if needed for the frontend.

requests: For making HTTP requests (if needed).

python-dotenv: To load environment variables from a .env file (for storing API keys or other sensitive data).